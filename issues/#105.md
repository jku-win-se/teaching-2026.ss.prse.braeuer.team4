The code coverage badge in the README.md file doesnt show the same coverage as mvn verify delivers because it shows coverage from the entire code and not only the non UI code. Make sure the coverage badge shows the same percentage as mvn verify.